---
- name: Running Nginx Reverse Proxy Playbook
  hosts: nginx
  vars_files:
    - variables/main.yml
  tasks:
    - name: Installing nginx
      become: true
      ansible.builtin.apt:
        name:
          - nginx
          - python3-cryptography

    - name: Enabling nginx
      become: true
      ansible.builtin.systemd_service:
        name: nginx
        state: started
        enabled: true

    - name: Adding site configuration
      ansible.builtin.include_tasks: tasks/nginx-reverse.yml
      loop: "{{ nginx.sites }}"

  handlers:
    - name: Restart nginx
      become: true
      ansible.builtin.systemd_service:
        name: nginx
        state: restarted
