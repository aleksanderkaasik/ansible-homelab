---
admin:
  user:
  key:
  password:

ansible:
  user:
  key:

root:
  key:

certificate:
  private_key: "{{ domain_name }}.key"
  csr: "{{ domain_name }}.csr"
  public_key: "{{ domain_name }}.crt"

  subject:
    country:
    state:
    city:
    organization:
    organizational_unit:
    common_name: "{{ domain_name }}"

bind9:
  tsig:
    name:
    algorithm:
    key:

  cidr_allowed_access_list: []
    # - 192.168.144.0/24
    # - 127.0.0.0/8

  forwarders: []
    # - 1.1.1.1
    # - 8.8.8.8
    # - 9.9.9.9

  forward_zones: []
    # - dev.home
    # - test.home

  reverse_zones: []
    # - 144.168.192.in-addr.arpa

url:
  get_ca: http://{{ pki.address }}:{{ pki.nodejs.port }}/get-ca
  post_sighed: http://{{ pki.address }}:{{ pki.nodejs.port }}/sign-ssl

pki:
  ca_cert_passphrase:
  address:
  nodejs:
    hostname:
    port:

mysql:
  username:
  password:
  mysql_host:
  privileges:
    # "*.*": "ALL,GRANT"

  address:
  port:

nextcloud:
  database:
    engine: mysql
    host: "{{ mysql.address }}"
    port: "{{ mysql.port }}"
    name: 
    user:
    password:

  admin:
    user:
    password:

  trusted_domains: []
    # - 127.0.0.1
    # - localhost
    # - "{{ ip_address }}"
    # - "{{ domain_name }}"

pterodactyl:
  panel:
    app:
      env:
      debug:
      key:
      theme:
      timezone:
      url:
      locale:
      environment_only:

    database:
      engine: mysql
      host: "{{ mysql.address }}"
      port: "{{ mysql.port }}"
      name:
      user:
      password:

    redis:
      host: 127.0.0.1
      port:
      password:

  wing:
    remote_server:

vault: # vaultwarden
  admin_token:

  db: # database
    engine: mysql
    host: "{{ mysql.address }}"
    port: "{{ mysql.port }}"
    name:
    user:
    password:

wordpress:
  database:
    engine: "mysql"
    host: "{{ mysql.address }}"
    port: "{{ mysql.port }}"
    name:
    user:
    password:
  # https://api.wordpress.org/secret-key/1.1/salt/
  auth_key:
  secure_auth_key:
  logged_in_key:
  nonce_key:
  auth_salt:
  secure_auth_salt:
  logged_in_salt:
  nonce_salt:

zabbix:
  server:
    database:
      engine: mysql
      host: "{{ mysql.address }}"
      port: "{{ mysql.port }}"
      name:
      user:
      password:
  agent:
    listening:
      server:
      port: 10050

snmp:
  community_string:
  listening_address:

haproxy:
  traffic: []
    # - name:
    #   address:
    #   domain:
    # - name:
    #   address:
    #   domain:

nginx:
  cloudflare:
    api_token:
    zones: []
      # "zone1.loc": "id1"
      # "zone2.loc": "id2"

  sites: []
    # - domain_name:
    #   proxy:
    # - domain_name:
    #   proxy:
